# Base service definitions shared by dev and prod.
# Run `docker compose up` for development (auto-merges docker-compose.override.yml).
# Run `docker compose -f docker-compose.yml -f docker-compose.prod.yml up --build` for production.

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
      - PROFILES_JSON_PATH=/data/profiles.json
      # Optional SMTP — leave unset to use log-only email (development default)
      # - SMTP_HOST=smtp.example.com
      # - SMTP_PORT=587
      # - SMTP_USER=user@example.com
      # - SMTP_PASSWORD=secret
      # - SMTP_FROM=noreply@bathaee.de
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 15s
      timeout: 5s
      retries: 3

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://backend:8000
    depends_on:
      backend:
        condition: service_healthy

networks:
  default:
    name: bathaee-net
